package interfaces;

/*
 * Interface para definir o padrão de métodos
 * que deverão ser implementados pelas classes
 * de validação do projeto
 */
public interface IValidation {

	// métodos abstratos
	boolean isValid(Object value);

}
